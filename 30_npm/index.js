// npm -v
// 전역(글로벌) 설치
// - npm i -g nodemon, npm i supervisor -g

// 지역(로컬) 설치
// package.json이라는 파일 안에 내가 불러쓰는 모듈들을 관리해야함.
// - npm init -> package.json 이라는 파일 자동 생성
// - npm i morgan
// - npm i body-parser --save
// - npm i mocha --save-dev(개발단계에서만 필요한 패키지일때 붙이는 옵션)
// - npm i -> package.json에 있는 패키지 설치 (의존성)
console.log("npm start");
